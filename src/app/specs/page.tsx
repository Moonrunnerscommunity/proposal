export default function SpecsPage() {
  // This page just shows a loading state while the layout redirects to README
  return (
    <div className="text-[#8a6fb7] flex items-center justify-center h-full">
      <p>Loading...</p>
    </div>
  );
}
